//var preco = 2;
var desconto = 0.2;
const PRECO = 2;
var total = PRECO - desconto;


//Funções

function soma(a, b) { //declaração de uma função
    console.log(a + b);
    return a + b;
}

soma(3, 5); //chamada de uma função

// Console

// Com o atalho ctrl + shift + i, vamos abrir a inspeção e após clicar em Console.

// Podemos trabalhar o JavaScript no console do navegador(cliente) ou pelo Node.JS(Servidor).

console.log("Hello World!");